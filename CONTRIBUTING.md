# Contributing

Contract changes must include tests, regenerated JSON Schemas, and a CHANGELOG
entry. Breaking field, type, enum, or event changes require a major version and
a migration plan for every official consumer. The package must remain free of
framework, persistence, network, and provider SDK dependencies.

